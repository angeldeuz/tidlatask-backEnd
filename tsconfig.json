{
  "compilerOptions": {
    "target": "ES2020",                     // Traduce a JS moderno
    "module": "CommonJS",                   // Usa módulos compatibles con Node.js
    "rootDir": "./src",                     // Carpeta de entrada para tu código fuente
    "outDir": "./dist",                     // Carpeta de salida para el JS compilado
    "esModuleInterop": true,                // Permite importar librerías que usan `require`
    "forceConsistentCasingInFileNames": true, // Evita errores por mayúsculas/minúsculas
    "strict": true,                         // Activa reglas estrictas (mejor para calidad)
    "skipLibCheck": true                    // No valida dependencias (ahorra tiempo de compilación)
  },
  "include": ["src"],
  "exclude": ["node_modules"]
}